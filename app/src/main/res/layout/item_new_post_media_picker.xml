<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="120dp"
    android:layout_margin="4dp">

    <ImageView
        android:id="@+id/media_picker_thumb"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/avatar_background"
        android:scaleType="centerCrop" />

    <ImageView
        android:id="@+id/media_picker_video_badge"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_gravity="bottom|end"
        android:layout_margin="8dp"
        android:padding="6dp"
        android:src="@drawable/ic_play"
        android:tint="@android:color/black"
        android:visibility="gone" />

    <TextView
        android:id="@+id/media_picker_video_duration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|start"
        android:layout_marginStart="8dp"
        android:layout_marginBottom="8dp"
        android:background="#AA000000"
        android:paddingStart="6dp"
        android:paddingTop="2dp"
        android:paddingEnd="6dp"
        android:paddingBottom="2dp"
        android:textColor="@android:color/white"
        android:textSize="11sp"
        android:textStyle="bold"
        android:visibility="gone" />

    <View
        android:id="@+id/media_picker_selected_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#662E6BFF"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/media_picker_selected_check"
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        android:src="@drawable/ic_check"
        android:tint="@android:color/white"
        android:visibility="gone" />

</FrameLayout>
